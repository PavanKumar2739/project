package testing;

public class Xpaths {
	//xpaths
	//button[contains(text(),'Res')]//parent::div
	//table[contains(@id,'contact')]//tr/td[contains(text(),'Mar')]/following-sibling::td ===> only inside with in a tag(tr)-- go forwards
	//table[contains(@id,'contact')]//tr/td[contains(text(),'Mar')]/following::td -->get all the things outside of the tag too
	
	//table[contains(@id,'contact')]//tr/td[contains(text(),'Mar')]/preceding-sibling::td ===> only inside with in a tag(tr)-- go forwards
	//table[contains(@id,'contact')]//tr/td[contains(text(),'Mar')]/preceding::td -->get all the things outside of the tag too
	
	//table[starts-with(@id,'contact')] check first
	//table[ends-with(@id,'end')]
	
}
